<? /* <div class="page-header">
	<h1><?php echo $ctx->page->title; ?></h1>
</div>
<div class="well">*/?>
	<?php foreach ($select as $id => $data): ?>
	<table class="table" id="categories">
		<thead>
			<tr>
				<th><?php echo HTML::icon('chevron-right'). ' '. HTML::anchor('action-group-edit/'.$id, $data['title']); ?></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($data['categories'] as $id => $title): ?>
		<tr>
			<td><?php echo HTML::icon('minus'). ' '. HTML::anchor('action-category-edit/'.$id, $title); ?>
      <?php echo HTML::anchor('backend/options/list/'.$id, '', array('class'=>"icon-list-alt icon")); ?>
      </td>
		</tr>
		<?php endforeach; ?>
		</tbody>
	</table>
	<?php endforeach; ?>
<? /*</div>*/?>